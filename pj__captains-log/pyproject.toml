[project]
name = "captains-log-sql"
version = "0.1.0"
description = "Local Postgres DDL and empty Neon publish for Captain's Log"
requires-python = ">=3.12"
dependencies = [
    "psycopg2-binary>=2.9,<3",
    "python-dotenv>=1.0,<2",
]
